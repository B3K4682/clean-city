export type IBlog = {
  id: number;
  image: string;
  title: string;
  shortDesc: string;
  desc: string;
  date: string;
};

export type HeroSlidesType = {
  id: number;
  title: string;
  titleEn: string;
  image: string;
  buttonText: string;
  buttonTextEn: string;
};

export type ServiceType = {
  id: number;
  title: string;
  titleEn: string;
  description: string;
  slug: string;
  image: string;
};
